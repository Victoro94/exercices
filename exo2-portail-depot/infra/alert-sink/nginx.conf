server {
  listen 80;
  # Sink d'alertes en dev : accepte les webhooks Alertmanager, loggue le payload.
  access_log /var/log/nginx/access.log;
  location / {
    return 200 'alert received\n';
  }
}
